{
    "name": "@grrr/contracts",
    "version": "0.0.0",
    "private": true,
    "main": "./src/index.ts",
    "types": "./src/index.ts",
    "dependencies": {
        "zod": "^3.22.0"
    },
    "scripts": {
        "build": "tsc",
        "type-check": "tsc --noEmit",
        "lint": "eslint src --ext ts",
        "test": "vitest run src"
    }
}